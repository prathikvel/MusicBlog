const typed = new Typed('#typed', {
    strings: ["One stop for all things music"],
    typeSpeed: 30,
    backSpeed: 40,
    startDelay: 200,
    smartBackspace: true,
    loop: false,
    showCursor: false
});